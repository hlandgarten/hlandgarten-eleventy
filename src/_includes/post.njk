{% extends "base.njk" %}

{% block content %}
<article class="post">
  <h1>{{ title }}</h1>
  <time datetime="{{ date }}">{{ date | readableDate }}</time>
  <div class="post-content">
    {{ content | safe }}
  </div>
</article>
{% endblock %}
